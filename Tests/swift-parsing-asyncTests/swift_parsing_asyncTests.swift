import XCTest
@testable import swift_parsing_async

final class swift_parsing_asyncTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_parsing_async().text, "Hello, World!")
    }
}
